//Let us write a simple code to print a decimal number to binary

var x : int;

read(x);

while(x > 0) {

    var a : int = x;

    while(a > 0) {
        a = a - 2;
    }
    if(a == 0) {
        print(0);
    }
    else {
        print (1);
    }
    x = x / 2;
}